---
const currentYear = new Date().getFullYear();
---

<footer class="footer">
  <div class="footer-container">
    <div class="footer-content">
      <div class="footer-section">
        <h3>Ahrens Optimate</h3>
        <p>AI-automatisering för infrastrukturprojekt</p>
      </div>

      <div class="footer-section">
        <h4>Kontakt</h4>
        <p>
          <strong>Lars Diethelm</strong><br />
          Lösningsarkitekt & förändringsledning<br />
          <a href="mailto:lars@ahrens.se">lars@ahrens.se</a><br />
          <a href="tel:+46702388999">0702-388 999</a>
        </p>
      </div>

      <div class="footer-section">
        <h4>Övriga kontakter</h4>
        <p>
          <strong>Michael Engström</strong><br />
          <a href="mailto:michael.engstrom@ahrens.se">michael.engstrom@ahrens.se</a><br />
          <a href="tel:+46763748769">0763-748 769</a>
        </p>
        <p>
          <strong>Ulf Christiansson</strong><br />
          <a href="mailto:ulf.christiansson@ahrens.se">ulf.christiansson@ahrens.se</a><br />
          <a href="tel:+46733118210">0733-118 210</a>
        </p>
      </div>
    </div>

    <div class="footer-bottom">
      <p>&copy; {currentYear} Ahrens Optimate. Alla rättigheter förbehållna.</p>
      <p class="footer-note">
        Dedikerad lösning för Implenia Sverige AB
      </p>
    </div>
  </div>
</footer>

<style>
  .footer {
    background: var(--color-gray-900);
    color: var(--color-white);
    padding: var(--space-3xl) 0 var(--space-xl);
    margin-top: var(--space-3xl);
  }

  .footer-container {
    max-width: var(--max-width-content);
    margin: 0 auto;
    padding: 0 var(--space-lg);
  }

  .footer-content {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: var(--space-2xl);
    margin-bottom: var(--space-2xl);
  }

  .footer-section h3 {
    color: var(--color-pink);
    font-size: var(--font-size-h3);
    margin-bottom: var(--space-md);
  }

  .footer-section h4 {
    color: var(--color-white);
    font-size: var(--font-size-body);
    font-weight: var(--font-weight-semibold);
    margin-bottom: var(--space-sm);
  }

  .footer-section p {
    color: var(--color-gray-300);
    line-height: var(--line-height-relaxed);
    margin-bottom: var(--space-md);
  }

  .footer-section a {
    color: var(--color-pink);
    text-decoration: none;
    transition: var(--transition-fast);
  }

  .footer-section a:hover {
    color: var(--color-white);
    text-decoration: underline;
  }

  .footer-bottom {
    border-top: 1px solid var(--color-gray-700);
    padding-top: var(--space-lg);
    text-align: center;
  }

  .footer-bottom p {
    color: var(--color-gray-400);
    font-size: var(--font-size-small);
    margin-bottom: var(--space-sm);
  }

  .footer-note {
    color: var(--color-gray-500);
    font-style: italic;
  }

  @media (max-width: 768px) {
    .footer-content {
      grid-template-columns: 1fr;
      gap: var(--space-xl);
    }
  }
</style>
